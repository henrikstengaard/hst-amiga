namespace Hst.Amiga.FileSystems.Pfs3
{
    public enum freeblocktype
    {
        /* keep or free anodes when freeing blocks */
        // enum freeblocktype {keepanodes, freeanodes};
        keepanodes,
        freeanodes
    }
}